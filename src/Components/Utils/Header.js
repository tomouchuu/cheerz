import styled from 'styled-components';

export const Header = styled.header`
  position: fixed;
  top: 0;
  right: 0;
  left: 0;
  z-index: 99;
  height: 60px;
  background: #fff;
`;

export default Header;